import React, { FC } from 'react';
import Leaflet from '../Leaflet';
import NavBar from '../Navbar';

const App: FC = () => (
    <>
        <NavBar />
        <Leaflet />
    </>
);

export default App;
